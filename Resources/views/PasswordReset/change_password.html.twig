{% extends '@FSiAdminSecurity/layout.html.twig' %}

{% block themes %}
    {% form_theme form 'bootstrap_3_layout.html.twig' %}
{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'class': 'form-signin'}}) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-lg btn-primary btn-block" value="{{ 'admin.password_reset.change_password.form.button.change_password'|trans({}, 'FSiAdminSecurity') }}" />
    {{ form_end(form) }}
{% endblock %}
